// Animacion de tabla historial
document.addEventListener('DOMContentLoaded', () => {
    const tabla = document.querySelector('.table');
    setTimeout(() => {
        tabla.style.opacity = '1';
    }, 5000);
});